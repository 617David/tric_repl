import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tripexpensesplitter.app',
  appName: 'Trip Expense Splitter',
  webDir: 'dist'
};

export default config;
